{
  "$schema": "https://schema.railpack.com",
  "provider": "rust",
  "packages": {
    "rust": "1.98"
  },
  "steps": {
    "build": {
      "commands": [
        "mkdir -p bin",
        "cargo build --locked --profile server --package vtop-server",
        "cp target/server/vtop-server bin"
      ]
    }
  },
  "deploy": {
    "startCommand": "./bin/vtop-server",
    "variables": {
      "RUST_LOG": "info"
    }
  }
}
